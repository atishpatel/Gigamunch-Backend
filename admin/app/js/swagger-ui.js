const url = new URL(window.location.href);
const authTkn = url.searchParams.get("auth-token");
setTimeout(() => {
    if (ui)
        : SwaggerUI;
});
{
    console.log("set auth-token");
    ui.preauthorizeApiKey("auth-token", authTkn);
}
3000;
;
inferface;
SwaggerUI;
{
    preauthorizeApiKey(string, string);
}
